import { TableHeadDirective } from './table-head.directive';

describe('TableHeadDirective', () => {
  it('should create an instance', () => {
    const directive = new TableHeadDirective();
    expect(directive).toBeTruthy();
  });
});
